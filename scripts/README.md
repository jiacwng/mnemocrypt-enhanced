# Scripts

| Folder | Contents |
|--------|----------|
| [`windows/`](windows/) | `.bat` shortcuts for the IDA + training pipeline on Windows |

Shell entry points remain at the repository root: `prepare_environment.sh`, `quick_start.sh`, plus `common/building_wrapper.sh` and `tool/plugin_batch.sh`.
